// const body = document.querySelector("body")!;
// body.style.setProperty("--mouse-x", "0");
// body.style.setProperty("--mouse-y", "0");
// const handlerMouseMove = (ev: MouseEvent) => {
//   const { x, y } = ev;
//   const { clientWidth, clientHeight } = body;
//   body.style.setProperty("--mouse-x", x / clientWidth - 0.5 + "");
//   body.style.setProperty("--mouse-y", -y / clientHeight + 0.5 + "");
// };
// window.addEventListener("mousemove", handlerMouseMove);

export {};
